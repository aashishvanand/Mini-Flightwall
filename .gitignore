# Real WiFi credentials -- never commit these
matrix64/**/secrets.h

# Personal n8n workflow configs (contain home coordinates / feeder details)
*_workflow.local.json

# macOS
.DS_Store

# Arduino build artifacts
*.hex
*.bin.build/
build/

.claude
.wrangler

tools/*.sh
# Local secrets (n8n API key, OpenSky client credentials)
.env
